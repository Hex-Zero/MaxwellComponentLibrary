export const transitions = { fast: '120ms ease-in-out', base: '200ms ease-in-out', slow: '320ms ease-in-out' };
export const durations = { instant: 0, fast: 120, base: 200, slow: 320 };
export const opacities = { disabled: 0.4, muted: 0.7, overlay: 0.5 };
export type Transitions = typeof transitions;
export type Durations = typeof durations;
export type Opacities = typeof opacities;
